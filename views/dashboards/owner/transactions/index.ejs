<% layout('layouts/boilerplate') %>
<% if (success.length) { -%>
	<%-include('../../../partials/success')%>
<% } -%>
<div class="row vh-90">
	<div class="col-12">
		<div class="card row">
			<div class="col-6 offset-3 mb-0 mt-2 card-title text-center">
				<p class="display-6 fw-bold m-0"><%= user.businessName%></p>
				<p class="display-6 fw-bold m-0">Transactions</p>
			</div>
			<div class="card-body">
					<div class="row mb-2">
						<div class="col-2">
							<form class="d-grid" action="/owner/transactions" method="GET">
								<button type="submit" name="prevOrNext" value="prev" class="btn btn-info mb-2">Previous</button>
							</form>
							<p class="btn btn-outline-secondary mb-2 d-grid"><%=startDate%></p>
							<h3>Date</h3>
							<% for(let item of sortedTransactions) { %>
							<p><a href="/owner/transactions/<%= item._id %>"><%= item.myDate %></a></p>
							<% } %>
						</div>
						<div class="col-8">
							<div class="row mb-2 justify-content-center">
								<form class="row row-cols align-items-center" action="/owner/transactions" method="GET">
									<div class="btn-group" role="group" aria-label="Basic example">
									  <button type="submit" name="viewType" value="thirty-day" class="btn btn-info w-100 mx-1">30-Day View</button>
									  <button type="submit" name="viewType" value="all" class="btn btn-info w-100 mx-1">All Transactions</button>
									  <button type="submit" name="viewType" value="monthly" class="btn btn-info w-100 mx-1">Monthly</button>
									</div>
								</form>
							</div>
							<div class="row mb-2 mx-1">
								<form action="/owner/transactions" method="GET">
									<div class="input-group">
										<span class="input-group-text">Filter By Client</span>
										<input type="text" class="form-control" placeholder="Client Name" aria-label="client name" name="client" value="<%=clientName%>">
										<div class="input-group-btn">
											<button class="btn btn-secondary">Filter</button>
										</div>
									</div>
								</form>
							</div>
							<h3 class="text-center mb-3">Description</h3>

							<% for(let item of sortedTransactions) { %>
								<% let final = "" %>
								<% item.lineItems.forEach(content => {%>
									<% final = content.lineItemContent + "," + final %>
								<% }) %>
								<p class="text-center">
									<%= item.client %>: <%= final.split(',').slice(0,-1).join(', ') %>
								</p>
							<% } %>
						</div>

						<div class="col-2">
							<form class="d-grid" action="/owner/transactions" method="GET">
								<button type="submit" name="prevOrNext" value="next" class="btn btn-info mb-2">Next</button>
							</form>
							<p class="btn btn-outline-secondary mb-2 d-grid"><%=endDate%></p>
							<h3>Total</h3>
							<% for(let item of sortedTransactions) { %>
								<p class="text-start">$<%=item.total%></p>
							<% } %>
						</div>
						<hr>
					</div>
					 
					<div class="row mb-2 inline-block">
						<div class="col-5 offset-5">
							<p class="display-6 fw-bold">TOTAL:</p>
						</div>
						<div class="col-2">
							<p class="text-start display-6 fw-bold">$<%= viewTotal %></p>
						</div>
					
					</div>
					<div class="row mb-2">
						<div class="d-grid">
							<a href="/owner/transactions/new" class="btn btn-success">Start a New Transaction</a>
						</div>
					</div>
			</div>
		</div>
	</div>
</div>